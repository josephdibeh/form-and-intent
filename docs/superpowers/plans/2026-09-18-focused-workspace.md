# Focused workspace implementation plan

Approved scope: explore → choose → refine; accessible component/token catalog; improve service-selection visual foundation before expanding screen families.

1. ArtifactView: focus selected result, reversible comparison mode. CompanionApp: choosing commits a revision; remove redundant layout acceptance; enable refinement of accepted work. Preserve existing history and contextual-copy safeguards.
2. SystemCatalog: native modal, component specimens from the actual renderer, semantic token specimens, matching Storybook links. One registered real system; no fake import or extra systems.
3. Next actions: deterministic context-aware supported prompts, staged in composer for editing rather than sent automatically. Preserve current artifact context.
4. Service selection: improve Choices presentation with comparison cards and readable hierarchy, responsive at narrow container widths. Do not invent facts or claim factual validation is solved.
5. Verify focused/reversible selection, catalog, request context, persistence and legacy regressions with tests, production build and browser checks. Record remaining AI factual-quality boundary.

Execution: steps 1–5 delivered and verified (105 tests, app/Storybook builds, browser selection/catalog/responsive checks). The single real system remains the only selectable entry. Service-selection presentation was improved within the existing contract; richer semantic primitives, structured-fact preservation and additional composition patterns remain a distinct next implementation stage. No public deployment performed.
